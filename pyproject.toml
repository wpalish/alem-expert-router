[project]
name = "alem-expert-router"
version = "0.3.0"
description = "Умное распределение экспертов, менторов и фрилансеров по заявкам"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
